/* eslint-disable react-refresh/only-export-components -- compatibility re-exports only */
// Compatibility exports retained for code written before the Phase 1 split.
export { AuthProvider } from "./AuthProvider";
export { useAuth } from "../hooks/useAuth";
